---
phase: 07-hero-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - public/hero-placeholder.jpg
autonomous: true

must_haves:
  truths:
    - "Visitor sees a full-viewport-height hero section with headline, description, and CTA button"
    - "Hero section displays a large image area with a testimonial card overlaid on the image"
    - "Clicking the CTA button smooth-scrolls to the #contact section"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Redesigned hero section with full-height layout, image, testimonial overlay, and anchor CTA"
      contains: "id=\"contact\""
    - path: "public/hero-placeholder.jpg"
      provides: "Placeholder hero image"
  key_links:
    - from: "Hero CTA button"
      to: "#contact section"
      via: "anchor href with smooth scroll"
      pattern: "href.*#contact"
---

<objective>
Redesign the hero section on the homepage to be full-height with a two-column layout: text content (headline, description, CTA) on the left, and an image with a testimonial card overlay on the right.

Purpose: Deliver the compelling first impression defined by HERO-01, HERO-02, and HERO-03 requirements using the monochrome design system already in place.
Output: Updated homepage hero section in page.tsx with placeholder image.
</objective>

<execution_context>
@/Users/robinkonijnendijk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinkonijnendijk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.tsx
@src/app/globals.css
@src/components/layout/header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder hero image</name>
  <files>public/hero-placeholder.jpg</files>
  <action>
    Generate a simple placeholder image for the hero section. Use a solid light gray (#e5e5e5) SVG converted to a placeholder, or download a free placeholder image. The image should be landscape-oriented, approximately 800x1000px (portrait-ish for the hero column).

    Simplest approach: create an SVG placeholder at `public/hero-placeholder.svg` instead of jpg. Make it a simple 800x1000 light gray rectangle with a subtle centered icon or text "Image" in slightly darker gray (#737373). This avoids needing external image downloads and keeps the repo self-contained.

    Update the files_modified note: use .svg extension if going SVG route.
  </action>
  <verify>File exists at public/hero-placeholder.svg (or .jpg). File is valid and non-empty.</verify>
  <done>A placeholder image file exists in public/ ready to be referenced by the hero section.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign hero section with full-height layout, image, and testimonial overlay</name>
  <files>src/app/page.tsx</files>
  <action>
    Replace the existing Hero section (lines 57-76 of page.tsx, the first `<section>` block) with a full-viewport-height hero section. Keep all other sections (Value Props, Product Showcase, How it Works, CTA Banner) untouched.

    **Layout structure:**
    - Full viewport height: `min-h-screen` on the hero section
    - Two-column layout on desktop (text left, image right), single column stacked on mobile
    - Use `mx-auto max-w-5xl` to match existing page width constraints
    - Account for fixed header offset (hero content should be vertically centered in viewport below header)

    **Left column — text content:**
    - Small uppercase label/tag above headline (e.g., "Premium Custom Textiles" in `text-sm font-semibold uppercase tracking-widest text-muted`)
    - Large headline: Use existing typographic style (`text-4xl font-light leading-[1.1] tracking-tight text-foreground sm:text-6xl lg:text-7xl`). Placeholder text like "Textiles, crafted to your exact dimensions" (can keep existing headline or similar)
    - Description paragraph: `text-lg text-muted sm:text-xl`, max-w-lg. Keep existing copy or similar placeholder.
    - CTA button: Change from `<Link href="/products/custom-textile">` to `<a href="#contact">`. Style as the existing accent button (`bg-accent px-8 py-3.5 text-sm font-medium tracking-wide text-accent-foreground`). Label: "Get in Touch" or "Request a Quote" with arrow. Uses native anchor for smooth scroll (per project decision 06-01).

    **Right column — image with testimonial overlay:**
    - Image container: relative positioned, takes up roughly half the width on desktop
    - Use `next/image` with the placeholder image (`/hero-placeholder.svg`). Set appropriate width/height, use `object-cover`, rounded corners or not (match monochrome aesthetic — no rounded corners aligns with current card style which uses sharp borders).
    - Testimonial card overlay: `absolute` positioned at bottom-left or bottom-right of the image container
    - Card styling: `bg-background/95 backdrop-blur-sm border border-border p-6` (matches header glass effect)
    - Card content: 5-star rating (simple text stars or SVG), placeholder quote text ("Absolutely perfect curtains, exactly the dimensions we needed."), author name and role in `text-sm text-muted`
    - Card should partially overlap the image edge for visual interest

    **Responsive behavior:**
    - Mobile: Stack vertically — text first, then image with overlay below
    - Desktop (md+): Side-by-side columns with `grid grid-cols-2 gap-12` or flexbox equivalent
    - Image section should have a reasonable height on mobile (e.g., `aspect-[4/5]` or min-h of 400px)

    **Design system compliance:**
    - Use ONLY existing CSS variables: `text-foreground`, `text-muted`, `bg-accent`, `text-accent-foreground`, `border-border`, `bg-surface`, `bg-background`
    - Follow monochrome aesthetic — no new colors
    - Match existing typography scale (text-sm, text-lg, text-4xl, etc.)
    - Keep consistent spacing with rest of page (px-6 for mobile padding)

    **Do NOT:**
    - Add scroll-reveal animations (out of scope — deferred to future milestone)
    - Import new dependencies
    - Modify any other section besides the Hero section
    - Use image carousel or multiple images (out of scope)
  </action>
  <verify>
    Run `npm run build` and confirm it completes without errors. Visually inspect the hero: it should span full viewport height, show headline + description + CTA on left, image with testimonial card on right (desktop), and stack vertically on mobile. The CTA `href` should be `#contact`.
  </verify>
  <done>
    - Hero section takes full viewport height (min-h-screen)
    - Headline, description, and CTA button are visible in the left column
    - CTA button has href="#contact" (scrolls to contact section via native smooth scroll)
    - Right column shows placeholder image with testimonial card overlay
    - Testimonial card shows star rating, quote, and author
    - Layout is responsive (stacks on mobile, side-by-side on desktop)
    - All other homepage sections remain unchanged
    - Build passes without errors
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Hero section spans full viewport height (min-h-screen)
3. Hero displays headline, description, and CTA button
4. Hero displays an image with testimonial card overlay (rating, quote, author)
5. CTA button href is "#contact" — clicking scrolls to contact section
6. Two-column layout on desktop (md+), stacked on mobile
7. Design uses existing monochrome color system (no new colors)
8. All other page sections (Value Props, Product Showcase, How it Works, CTA Banner) unchanged
</verification>

<success_criteria>
- HERO-01: Full-height hero with headline, description, CTA — verified by min-h-screen + visible text content + CTA button
- HERO-02: Image with testimonial card overlay — verified by placeholder image + overlay card with rating/quote/author
- HERO-03: CTA scrolls to contact — verified by href="#contact" + existing smooth scroll CSS
- Build passes: `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-hero-section/07-01-SUMMARY.md`
</output>
