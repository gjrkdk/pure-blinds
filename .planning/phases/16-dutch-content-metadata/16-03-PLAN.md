---
phase: 16-dutch-content-metadata
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/breadcrumbs.tsx
  - src/app/cart/page.tsx
  - src/components/cart/cart-item.tsx
  - src/components/cart/cart-summary.tsx
  - src/components/cart/remove-dialog.tsx
  - src/app/confirmation/page.tsx
autonomous: true

must_haves:
  truths:
    - "Header navigation links display Dutch labels (Producten, Blog)"
    - "Footer displays Dutch link labels and copyright text"
    - "Cart page title, empty state, and continue shopping link are in Dutch"
    - "Cart item labels (each, Remove) are in Dutch"
    - "Cart summary labels (Order Summary, Subtotal, Shipping, Total, Proceed to Checkout) are in Dutch"
    - "Remove dialog text is in Dutch"
    - "Confirmation page displays Dutch thank-you text and next steps"
    - "Cart page and confirmation page have unique Dutch metadata with OG nl_NL"
    - "Breadcrumb ARIA label is in Dutch"
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "Dutch nav labels"
      contains: "Producten"
    - path: "src/components/cart/cart-summary.tsx"
      provides: "Dutch cart summary labels"
      contains: "Besteloverzicht"
    - path: "src/app/confirmation/page.tsx"
      provides: "Dutch confirmation content and metadata"
      contains: "Bedankt voor uw bestelling"
  key_links:
    - from: "src/app/cart/page.tsx"
      to: "src/components/cart/cart-summary.tsx"
      via: "CartSummary component"
      pattern: "CartSummary"
    - from: "src/app/cart/page.tsx"
      to: "src/components/cart/cart-item.tsx"
      via: "CartItem component"
      pattern: "CartItem"
---

<objective>
Localize all navigation, cart, and confirmation page UI strings to Dutch, and add SEO metadata to cart and confirmation routes.

Purpose: UI chrome (header, footer, cart, confirmation) must present a consistent Dutch experience. Mixed English/Dutch UI breaks user trust.
Output: All interface elements outside homepage and product pages display Dutch text with proper metadata.
</objective>

<execution_context>
@/Users/robinkonijnendijk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinkonijnendijk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-dutch-content-metadata/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header, footer, and breadcrumbs Dutch localization</name>
  <files>src/components/layout/header.tsx, src/components/layout/footer.tsx, src/components/layout/breadcrumbs.tsx</files>
  <action>
**header.tsx:**
1. Replace navLinks labels:
   - "Products" -> "Producten"
   - "Blog" -> "Blog" (same in Dutch)
2. Replace aria-label: "Toggle menu" -> "Menu openen" (or "Menu" for brevity)
3. Keep brand name "Pure Blinds" as-is (brand names don't translate)

**footer.tsx:**
1. Replace links array labels:
   - "Roller Blinds" -> "Rolgordijnen"
   - "Blog" -> "Blog" (same)
   - "About us" -> "Over ons"
   - "Contact" -> "Contact" (same in Dutch)
2. Replace "Quick links" heading -> "Snelle links"
3. Replace copyright text: "All rights reserved." -> "Alle rechten voorbehouden."
4. Replace tagline: "Custom dimension roller blinds" -> "Rolgordijnen op maat"
5. Keep brand name "Pure Blinds" as-is

**breadcrumbs.tsx:**
1. Replace aria-label: "Breadcrumb" -> "Kruimelpad" (standard Dutch accessibility term for breadcrumbs)
2. Note: The actual breadcrumb label text ("Home", "Products", etc.) is passed in via props from each page - those are handled in the respective page plans (01, 02, 04). The breadcrumbs component itself only needs the aria-label changed.

Important: Keep all component logic, event handlers, state, and styling exactly as-is. Only change text strings.
  </action>
  <verify>Run `npx next build` - should compile without errors. Grep for "Products" in header.tsx navLinks (should find "Producten" instead). Grep for "Quick links" in footer.tsx (should find "Snelle links"). Grep for "All rights reserved" in footer.tsx (should find none).</verify>
  <done>Header shows Dutch navigation labels. Footer displays Dutch link text, heading, and copyright. Breadcrumbs have Dutch ARIA label.</done>
</task>

<task type="auto">
  <name>Task 2: Cart page, cart components, and confirmation page Dutch localization + metadata</name>
  <files>src/app/cart/page.tsx, src/components/cart/cart-item.tsx, src/components/cart/cart-summary.tsx, src/components/cart/remove-dialog.tsx, src/app/confirmation/page.tsx</files>
  <action>
**src/app/cart/page.tsx:**
1. Add metadata export (outside the component, since this is a client component, use a separate layout.tsx or add metadata via a wrapper pattern). Actually, since this is a 'use client' component, metadata cannot be exported directly. Instead, create a new file `src/app/cart/layout.tsx` that exports the metadata:
   ```typescript
   import type { Metadata } from 'next'

   export const metadata: Metadata = {
     title: 'Winkelwagen | Pure Blinds',
     description: 'Bekijk en beheer uw geselecteerde rolgordijnen op maat. Ga verder naar de kassa om uw bestelling af te ronden.',
     openGraph: {
       locale: 'nl_NL',
       type: 'website',
       title: 'Winkelwagen | Pure Blinds',
       description: 'Bekijk en beheer uw geselecteerde rolgordijnen op maat.',
       siteName: 'Pure Blinds',
     },
     robots: { index: false },
   }

   export default function CartLayout({ children }: { children: React.ReactNode }) {
     return children
   }
   ```
   Note: Add `robots: { index: false }` since cart pages shouldn't be indexed.
2. Replace cart page strings:
   - "Your Cart" (3 occurrences) -> "Uw winkelwagen"
   - "Your cart is empty" -> "Uw winkelwagen is leeg"
   - "Start configuring" -> "Begin met configureren"
   - "Continue shopping" -> "Verder winkelen"

**src/components/cart/cart-item.tsx:**
1. Replace "each" -> "per stuk"
2. Replace "Remove" button text -> "Verwijderen"

**src/components/cart/cart-summary.tsx:**
1. Replace "Order Summary" -> "Besteloverzicht"
2. Replace "Subtotal ({count} {item/items})" logic:
   - 'item' -> 'artikel'
   - 'items' -> 'artikelen'
   So: `Subtotaal ({itemCount} {itemCount === 1 ? 'artikel' : 'artikelen'})`
3. Replace "Shipping" -> "Verzending"
4. Replace "Calculated at checkout" -> "Berekend bij afrekenen"
5. Replace "Total" -> "Totaal"
6. Replace "Proceed to Checkout" -> "Afrekenen"
7. Replace "Preparing checkout..." -> "Bestelling voorbereiden..."
8. Replace error message: "Unable to process checkout. Please try again." -> "Kan bestelling niet verwerken. Probeer het opnieuw."

**src/components/cart/remove-dialog.tsx:**
1. Replace dialog title: "Remove from cart?" -> "Verwijderen uit winkelwagen?"
2. Replace dialog description: "Remove {productName} from your cart?" -> "{productName} verwijderen uit uw winkelwagen?"
3. Replace "Cancel" button -> "Annuleren"
4. Replace "Remove" button -> "Verwijderen"

**src/app/confirmation/page.tsx:**
1. Add metadata export at top of file:
   ```typescript
   import type { Metadata } from 'next'

   export const metadata: Metadata = {
     title: 'Bestelling Bevestigd | Pure Blinds',
     description: 'Uw bestelling voor rolgordijnen op maat is bevestigd. Bekijk de volgende stappen.',
     openGraph: {
       locale: 'nl_NL',
       type: 'website',
       title: 'Bestelling Bevestigd | Pure Blinds',
       description: 'Uw bestelling voor rolgordijnen op maat is bevestigd.',
       siteName: 'Pure Blinds',
     },
     robots: { index: false },
   }
   ```
2. Replace all content strings:
   - h1: "Thank you for your order" -> "Bedankt voor uw bestelling"
   - "Order {order_id}" -> "Bestelling {order_id}"
   - Description: "Your custom roller blind is on its way to being made." -> "Uw rolgordijn op maat wordt nu geproduceerd."
   - "What happens next" -> "Wat er nu gebeurt"
   - Step 01: "Order confirmation" / "You'll receive an email..." -> "Bevestiging" / "U ontvangt binnenkort een e-mail met uw bestelgegevens."
   - Step 02: "Production" / "Your roller blind is cut..." -> "Productie" / "Uw rolgordijn wordt op uw exacte maten gesneden en afgewerkt."
   - Step 03: "Shipping" / "Once ready, your order is packed..." -> "Verzending" / "Zodra gereed wordt uw bestelling ingepakt en naar uw adres verzonden."
   - "Back to home" button -> "Terug naar home"

Important: Keep all component logic, state management, Shopify checkout integration, and styling exactly as-is. For the cart page metadata, create a new layout.tsx file since the cart page is a client component and cannot export metadata directly.

**New file to add: src/app/cart/layout.tsx**
  </action>
  <verify>Run `npx next build` - should compile without errors. Grep for "Your Cart", "Order Summary", "Proceed to Checkout", "Remove from cart", "Thank you for your order" in modified files (should find none). Verify cart/layout.tsx exists and exports metadata. Verify confirmation page has metadata export.</verify>
  <done>Cart page, all cart components, remove dialog, and confirmation page display Dutch text. Cart and confirmation routes have metadata with OG nl_NL locale. Both have robots noindex.</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. Header navigation shows "Producten" and "Blog"
3. Footer shows Dutch links and copyright
4. Cart page shows "Uw winkelwagen" and Dutch labels throughout
5. Cart summary shows "Besteloverzicht", "Subtotaal", "Afrekenen"
6. Remove dialog shows Dutch text
7. Confirmation page shows Dutch thank-you and next steps
8. Cart and confirmation routes have metadata with OG nl_NL
9. Breadcrumbs aria-label is "Kruimelpad"
</verification>

<success_criteria>
- Header, footer, breadcrumbs display Dutch text
- Cart page title, empty state, and all cart component labels are in Dutch
- Cart summary shows Dutch labels (Besteloverzicht, Subtotaal, Verzending, Totaal, Afrekenen)
- Remove dialog shows Dutch confirmation text
- Confirmation page shows Dutch thank-you and next steps
- Cart and confirmation pages have unique Dutch metadata with OG nl_NL
- Build succeeds with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-dutch-content-metadata/16-03-SUMMARY.md`
</output>
