---
phase: 10-support-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/faq-section.tsx
  - src/components/home/contact-section.tsx
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor sees FAQ section with two-column layout showing intro text on left and accordion on right"
    - "Each FAQ item expands/collapses on click with plus/minus icon animation"
    - "Visitor sees contact section with business info including address, email, phone, and social links"
    - "Contact form displays name, email, phone, and message fields"
    - "Form validates required fields (name, email, message) and shows inline error messages before submission"
  artifacts:
    - path: "src/components/home/faq-section.tsx"
      provides: "FAQ two-column layout with accordion"
      contains: "FaqSection"
    - path: "src/components/home/contact-section.tsx"
      provides: "Contact info and validated form"
      contains: "ContactSection"
    - path: "src/app/page.tsx"
      provides: "Homepage with FAQ and Contact sections integrated"
      contains: "FaqSection"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/home/faq-section.tsx"
      via: "import and render"
      pattern: "import.*FaqSection"
    - from: "src/app/page.tsx"
      to: "src/components/home/contact-section.tsx"
      via: "import and render"
      pattern: "import.*ContactSection"
    - from: "src/components/home/contact-section.tsx"
      to: "#contact anchor"
      via: "section id attribute"
      pattern: "id=\"contact\""
---

<objective>
Add FAQ section with two-column accordion layout and Contact section with business info, form, and client-side validation to complete the homepage.

Purpose: Complete the final homepage sections so visitors can find answers to common questions and reach the business through multiple channels. This is the last phase of the v1.1 Design Homepage milestone.
Output: Two new homepage components (faq-section.tsx, contact-section.tsx) integrated into page.tsx, replacing the existing CTA Banner with a proper contact section.
</objective>

<execution_context>
@/Users/robinkonijnendijk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinkonijnendijk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase patterns needed
@src/components/home/services-accordion.tsx — Accordion pattern (useState toggle, plus icon rotation, max-height transition)
@src/app/page.tsx — Current homepage structure and section order
@src/app/globals.css — Design tokens (--foreground, --muted, --border, --accent, etc.)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FAQ section with two-column layout and accordion</name>
  <files>src/components/home/faq-section.tsx</files>
  <action>
Create a client component `FaqSection` in `src/components/home/faq-section.tsx` with `'use client'` directive.

**Layout structure:**
- Section with `id="faq"` and consistent padding (`px-6 py-20 sm:py-28`) matching other sections
- `max-w-5xl mx-auto` container
- Two-column layout using `lg:grid lg:grid-cols-2 lg:gap-16`
- Left column: Section intro — subtitle ("Frequently Asked Questions" in `text-sm font-semibold uppercase tracking-widest text-muted`), heading (`text-3xl font-light tracking-tight`), and brief paragraph in `text-muted`
- Right column: FAQ accordion items

**Accordion (reuse Phase 8 pattern from services-accordion.tsx):**
- `useState<number | null>(0)` — first item open by default (per 08-01 decision)
- 6 FAQ items as const array with `question` and `answer` fields
- Placeholder content for questions (e.g., "What dimensions can I order?", "How does pricing work?", "What fabrics are available?", "How long does delivery take?", "Can I return custom orders?", "Do you offer bulk discounts?")
- Each item: button with question text + plus/minus SVG icon
- Plus icon: same SVG as services-accordion (line x1="12" y1="5" x2="12" y2="19" + line x1="5" y1="12" x2="19" y2="12"), rotates 90deg when open via `transition-transform duration-300`
- Expandable answer: `overflow-hidden transition-all duration-300` with `maxHeight` toggling between "10rem" and "0"
- Answer text in `text-sm leading-relaxed text-muted` with `py-4` padding
- Border-bottom on each item button: `border-b border-border`

**Naming convention:** Follow kebab-case file, PascalCase export, camelCase functions.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Visually confirm: two-column layout on desktop, stacked on mobile, accordion items toggle open/close.</verify>
  <done>FAQ section renders with two-column layout (intro left, accordion right on lg+), 6 FAQ items with expand/collapse on click, plus icon rotates on open, first item open by default.</done>
</task>

<task type="auto">
  <name>Task 2: Create Contact section with business info and validated form, integrate both sections into homepage</name>
  <files>src/components/home/contact-section.tsx, src/app/page.tsx</files>
  <action>
**Part A — Create ContactSection component:**

Create a client component `ContactSection` in `src/components/home/contact-section.tsx` with `'use client'` directive.

**Layout structure:**
- Section with `id="contact"` (takes over anchor from current CTA Banner — hero CTA links to `#contact`)
- Light background (default `bg-background`) with `border-t border-border` top separator, consistent padding (`px-6 py-20 sm:py-28`)
- `max-w-5xl mx-auto` container
- Section header: subtitle ("Get in Touch" in `text-sm font-semibold uppercase tracking-widest text-muted`) + heading (`text-3xl font-light tracking-tight`)
- Two-column grid on lg+: `lg:grid lg:grid-cols-2 lg:gap-16`
- Left column: Contact form
- Right column: Business info

**Contact form (left column):**
- State: `useState` for form data (`{ name: '', email: '', phone: '', message: '' }`), errors (`Record<string, string>`), and submitted boolean
- Fields: Name (text, required), Email (email, required), Phone (tel, optional), Message (textarea, required)
- Input styling: `w-full border border-border bg-background px-4 py-3 text-sm text-foreground placeholder:text-muted/60 focus:outline-none focus:border-foreground transition-colors`
- Label styling: `block text-sm font-medium text-foreground mb-2`
- Error styling: `text-xs text-red-500 mt-1`
- Validation on submit (`handleSubmit`):
  - Name: required, show "Name is required"
  - Email: required + basic format check (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`), show "Email is required" or "Please enter a valid email"
  - Message: required, show "Message is required"
  - Phone: no validation (optional field)
  - If errors exist, set error state and return early (do not submit)
  - If valid: set `submitted` to true and show a success message ("Thank you for your message. We'll get back to you soon.") — no actual backend call (contact form backend is out of scope)
- Submit button: `w-full bg-accent text-accent-foreground py-3 text-sm font-medium tracking-wide transition-opacity hover:opacity-80` with text "Send Message"
- Form fields spaced with `space-y-6`

**Business info (right column):**
- Office address block: heading "Our Office" in `text-sm font-semibold uppercase tracking-widest text-foreground`, address below in `text-sm text-muted leading-relaxed` (placeholder: "Keizersgracht 123\n1015 CJ Amsterdam\nThe Netherlands")
- Contact details block: "Contact" heading same style, then email and phone as anchor links (`<a href="mailto:...">` and `<a href="tel:...">`) in `text-sm text-muted hover:text-foreground transition-colors`
  - Email: info@pureblinds.nl
  - Phone: +31 20 123 4567
- Social links block: "Follow Us" heading same style, then row of 4 social icon links (Instagram, Facebook, Pinterest, LinkedIn) as SVG icons in `text-muted hover:text-foreground transition-colors`
  - Use simple, recognizable SVG paths for each (16x16 or 20x20 viewBox)
  - Wrap each in `<a href="#" target="_blank" rel="noopener noreferrer">` with `aria-label`
- Stack blocks vertically with `space-y-8`

**Part B — Integrate into homepage (src/app/page.tsx):**

1. Add imports for `FaqSection` and `ContactSection` from `@/components/home/`
2. Insert `<FaqSection />` after `<TestimonialsSection />` with comment `{/* FAQ */}`
3. Insert `<ContactSection />` after FAQ section with comment `{/* Contact */}`
4. Remove the existing CTA Banner section entirely (the one with `id="contact"` and "Ready to get started?" text) — ContactSection now owns the `#contact` anchor
5. Remove the unused `Link` import from "next/link" if no longer used
6. Remove the unused `Image` import from "next/image" if no longer used (hero section uses Image, so keep it if hero still references it)
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors. Run `npm run build` — builds successfully. Visually confirm: contact form shows all 4 fields, validation errors appear for empty required fields on submit, success message shows on valid submit, business info displays address/email/phone/socials, hero CTA "#contact" scrolls to new contact section.</verify>
  <done>Contact section renders with validated form (name, email, message required; phone optional) and business info (address, email, phone, socials). FAQ and Contact sections integrated into homepage between Testimonials and page end. CTA Banner replaced. Hero "#contact" anchor correctly targets new contact section.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. `npm run build` completes successfully
3. Homepage section order: Hero > About > Services > Our Work > Testimonials > FAQ > Contact
4. FAQ section: two-column layout (intro + accordion) on desktop, stacked on mobile
5. FAQ accordion: items expand/collapse on click with plus icon animation
6. Contact form: validates name, email, message as required; shows inline errors; shows success on valid submit
7. Contact business info: address, email (mailto), phone (tel), social icon links
8. Hero CTA "Get in Touch" scrolls to #contact (the new ContactSection)
</verification>

<success_criteria>
- All 5 phase success criteria met:
  1. FAQ section with two-column layout (intro + accordion) visible
  2. Each FAQ expands/collapses with plus/minus icons
  3. Contact section shows business info (address, email, phone, socials)
  4. Contact form has name, email, phone, and message fields
  5. Form validates required fields before submission
- Build passes without errors
- No type errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-support-contact/10-01-SUMMARY.md`
</output>
