---
phase: 25-cookie-consent-banner
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/privacybeleid/page.tsx
autonomous: false
requirements: [CONS-01]

must_haves:
  truths:
    - "A /privacybeleid page exists with Dutch privacy policy content covering GA4 analytics cookies"
    - "The banner's 'Privacybeleid' link navigates to /privacybeleid and displays the policy page"
    - "The privacy page is visually consistent with the site's existing page layouts"
  artifacts:
    - path: "src/app/privacybeleid/page.tsx"
      provides: "Static Dutch privacy policy page with Metadata export"
      min_lines: 60
  key_links:
    - from: "cookie-consent-banner description HTML"
      to: "src/app/privacybeleid/page.tsx"
      via: "href='/privacybeleid' anchor in banner copy"
      pattern: "/privacybeleid"
---

<objective>
Create the /privacybeleid static page with a standard Dutch privacy and cookie policy, then visually verify the complete consent banner + privacy page flow.

Purpose: The consent banner links to /privacybeleid for GDPR transparency. This page provides the legally required privacy policy disclosure covering GA4 analytics cookie usage.

Output: A /privacybeleid page matching site design, containing Dutch privacy policy sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing page pattern to follow — from src/app/inmeetinstructies/page.tsx -->

Page structure pattern:
```typescript
import Breadcrumbs from "@/components/layout/breadcrumbs";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "... | Pure Blinds",
  description: "...",
  openGraph: { locale: "nl_NL", type: "website", ... },
};

export default function PageName() {
  return (
    <div className="px-6 py-12 sm:py-16">
      <div className="mx-auto max-w-5xl">
        <Breadcrumbs items={[{ label: "Home", href: "/" }, { label: "...", current: true }]} />
        {/* Content */}
      </div>
    </div>
  );
}
```

From src/components/layout/breadcrumbs.tsx:
```typescript
export default function Breadcrumbs({ items }: { items: Array<{ label: string; href?: string; current?: boolean }> })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /privacybeleid privacy policy page</name>
  <files>
    src/app/privacybeleid/page.tsx
  </files>
  <action>
Create `src/app/privacybeleid/page.tsx` as a server component (NO 'use client'). Follow the same page structure pattern as `src/app/inmeetinstructies/page.tsx`.

The page must include:
- `Metadata` export with Dutch title "Privacybeleid | Pure Blinds" and appropriate description
- `openGraph` metadata with `locale: "nl_NL"`
- `Breadcrumbs` component with Home > Privacybeleid
- Content wrapper: `max-w-2xl` inner container for readable line length (like the research suggested)

Privacy policy sections (all in friendly, casual Dutch matching the site tone):

1. **Wie zijn wij** — Pure Blinds (pure-blinds.nl) sells custom roller blinds online. Keep it brief.

2. **Welke gegevens verzamelen we** — Explain that the site uses Google Analytics 4 to understand how visitors use the site. Mention that this involves analytics cookies (_ga, _gid). Be honest and simple.

3. **Cookies** — Explain what cookies are used:
   - Necessary cookies: session/cart functionality (no consent needed)
   - Analytics cookies: Google Analytics 4 — only placed after explicit consent via the cookie banner
   - No marketing or advertising cookies

4. **Toestemming** — Explain the consent mechanism: visitors can accept or reject analytics cookies via the banner. Rejecting does not affect site functionality. Consent is stored for 12 months.

5. **Hoe lang bewaren we gegevens** — Google Analytics data retention period (14 months default). Consent choice stored 12 months.

6. **Uw rechten** — Under GDPR/AVG: right to access, correction, deletion, objection. Can withdraw consent by clearing browser data.

7. **Contact** — Email address info@pure-blinds.nl for privacy questions.

Style the page using:
- `prose` class from `@tailwindcss/typography` for body text (the plugin is already installed — see globals.css `@plugin "@tailwindcss/typography"`)
- Use `<h1>` for page title, `<h2>` for section headings
- Keep the same visual style as other content pages (font-light headings, text-muted for subtitles)
- Use semantic HTML: `<section>`, `<h2>`, `<p>`, `<ul>` as appropriate

Do NOT include a "Laatst bijgewerkt" date or version number — keep it simple and maintenance-free.
  </action>
  <verify>
    <automated>cd /Users/robinkonijnendijk/Desktop/projects/pure-blinds && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - /privacybeleid page exists at src/app/privacybeleid/page.tsx
    - Page is a server component with Metadata export
    - Content covers all 7 Dutch privacy policy sections
    - Uses Breadcrumbs component with Home > Privacybeleid
    - Uses @tailwindcss/typography prose class for content
    - Build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete cookie consent banner flow:
    1. Dutch-language consent banner at bottom of page with "Accepteer alles" and "Weiger alles" buttons
    2. CSS styling matched to site design (colors, fonts, no border radius)
    3. Privacy policy page at /privacybeleid linked from the banner
    4. Consent stored in localStorage (not a cookie)
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000 in an incognito/private window
    2. Verify: A consent banner appears at the bottom of the page
    3. Verify: Banner text is in Dutch with "Accepteer alles" and "Weiger alles" buttons
    4. Verify: Both buttons are the same size and visual prominence (no dark pattern)
    5. Verify: You can scroll the page and interact with it while the banner is showing
    6. Verify: Click the "Privacybeleid" link — it opens the /privacybeleid page with Dutch privacy policy content
    7. Go back, then open DevTools > Application > Local Storage > localhost
    8. Click "Weiger alles" — verify banner disappears, no `_ga` cookie in Application > Cookies
    9. Verify: `cc_cookie` key appears in localStorage with consent data
    10. Refresh the page — verify banner does NOT re-appear
    11. Clear localStorage, refresh — verify banner appears again
    12. Click "Accepteer alles" — verify banner disappears
    13. Check DevTools > Application > Cookies — if GA_MEASUREMENT_ID is set, `_ga` cookie should appear after accepting
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the banner appearance, behavior, or privacy page content</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `/privacybeleid` page renders with all 7 Dutch privacy policy sections
3. Banner "Privacybeleid" link navigates to /privacybeleid
4. Page follows site's existing layout pattern (Breadcrumbs, max-w container, consistent typography)
5. Visual verification confirms banner appearance, button equality, and consent flow
</verification>

<success_criteria>
- /privacybeleid page exists with complete Dutch privacy policy
- Banner links to /privacybeleid and it loads correctly
- Visual verification of complete consent flow passes (banner, buttons, persistence, privacy page)
</success_criteria>

<output>
After completion, create `.planning/phases/25-cookie-consent-banner/25-02-SUMMARY.md`
</output>
