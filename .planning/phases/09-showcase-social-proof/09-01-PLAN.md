---
phase: 09-showcase-social-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/work-section.tsx
  - src/components/home/testimonials-section.tsx
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor sees Our Work section with project showcase cards"
    - "Projects display with alternating image/content layouts (image left then right)"
    - "Each project shows a category tag, duration tag, and inline testimonial quote"
    - "Visitor sees testimonials section with dark background"
    - "Testimonials display in a responsive grid with star ratings"
  artifacts:
    - path: "src/components/home/work-section.tsx"
      provides: "Our Work showcase with alternating project cards"
      contains: "id=\"work\""
    - path: "src/components/home/testimonials-section.tsx"
      provides: "Testimonials grid on dark background"
      contains: "id=\"testimonials\""
    - path: "src/app/page.tsx"
      provides: "Homepage integrating work and testimonials sections"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/home/work-section.tsx"
      via: "import and render"
      pattern: "import.*WorkSection"
    - from: "src/app/page.tsx"
      to: "src/components/home/testimonials-section.tsx"
      via: "import and render"
      pattern: "import.*TestimonialsSection"
---

<objective>
Add Our Work showcase section and Testimonials section to the homepage.

Purpose: Provide social proof and portfolio showcase so visitors can see completed projects and customer reviews, building trust before contacting the business.
Output: Two new homepage sections — project showcase with alternating layouts and testimonials grid on dark background.
</objective>

<execution_context>
@/Users/robinkonijnendijk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinkonijnendijk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/page.tsx
@src/app/globals.css
@src/components/home/about-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Our Work showcase section with alternating project cards</name>
  <files>src/components/home/work-section.tsx, src/app/page.tsx</files>
  <action>
Create `src/components/home/work-section.tsx` as a server component (no 'use client' needed — static content only).

**Section structure:**
- Section with `id="our-work"`, standard spacing (`px-6 py-20 sm:py-28`), `border-t border-border`
- Container `mx-auto max-w-5xl`
- Label: `text-sm font-semibold uppercase tracking-widest text-muted` with text "Our Work"
- Heading: `text-3xl font-light tracking-tight text-foreground sm:text-4xl` with text "Projects we're proud of"
- Optional subtext in `text-muted`

**Project cards data array (3 projects):**
Each project object has: `title`, `category` (tag text like "Residential"), `duration` (tag text like "2 weeks"), `description` (1-2 sentences), `testimonial` (short quote), `author` (name), `authorRole` (role text).

Example projects:
1. Category: "Residential" / Duration: "2 weeks" / Title: "Modern Living Room Curtains" — custom curtains for a family home
2. Category: "Commercial" / Duration: "3 weeks" / Title: "Restaurant Privacy Screens" — room dividers for a restaurant
3. Category: "Events" / Duration: "1 week" / Title: "Corporate Exhibition Backdrop" — branded backdrop for trade show

**Alternating layout pattern:**
Map over projects with index. Use a grid: `grid md:grid-cols-2 gap-8 lg:gap-12 items-center`.
- Even index (0, 2): image on LEFT, content on RIGHT
- Odd index (1): image on RIGHT, content on LEFT — use `md:order-2` on image div and `md:order-1` on content div for odd items

**Image placeholder:** Each project shows a `div` with `aspect-[4/3] w-full bg-surface border border-border rounded-lg` containing centered muted text "Project Image" (placeholder for real photos). Do NOT use next/image or SVG files — just a styled placeholder div.

**Content side for each card:**
- Category + duration tags: Two inline `span` elements styled as small tags — `text-xs font-medium uppercase tracking-wider px-2.5 py-1 border border-border text-muted` with a small gap between them
- Title: `text-xl font-medium text-foreground sm:text-2xl mt-4`
- Description: `text-sm leading-relaxed text-muted mt-3`
- Inline testimonial: A blockquote area with `mt-6 border-l-2 border-border pl-4` containing the quote in `text-sm italic text-muted` and the author name/role below in `text-xs text-muted mt-2` formatted as "— Author Name, Role"

**Spacing between cards:** Use `space-y-16 sm:space-y-20 mt-14` on the cards container.

**In `src/app/page.tsx`:**
- Add import: `import { WorkSection } from "@/components/home/work-section";`
- Replace the existing "How it Works" section (the Process/STEPS section) with `<WorkSection />`
- Keep the section order: Hero > About > Services > Our Work > CTA Banner
  </action>
  <verify>Run `npm run build` — must exit 0 with no errors. Verify work-section.tsx exists and page.tsx imports it.</verify>
  <done>Our Work section renders 3 project cards with alternating image/content layout, each showing category tag, duration tag, description, and inline testimonial quote.</done>
</task>

<task type="auto">
  <name>Task 2: Create Testimonials section with dark background and star ratings grid</name>
  <files>src/components/home/testimonials-section.tsx, src/app/page.tsx</files>
  <action>
Create `src/components/home/testimonials-section.tsx` as a server component (no 'use client' needed — static content only).

**Section structure:**
- Section with `id="testimonials"`, dark background using `bg-foreground` (same pattern as existing CTA Banner section which uses `bg-foreground` for dark backgrounds), padding `px-6 py-20 sm:py-28`
- Container `mx-auto max-w-5xl`
- Label: `text-sm font-semibold uppercase tracking-widest text-neutral-400` with text "Testimonials"
- Heading: `text-3xl font-light tracking-tight text-accent-foreground sm:text-4xl` with text "What our clients say"
- Note: On dark background, use `text-accent-foreground` for headings (white), `text-neutral-400` for muted text, `text-neutral-300` for body text — following the existing CTA Banner color pattern

**Testimonials data array (6 testimonials):**
Each testimonial object has: `rating` (number 4 or 5), `quote` (1-2 sentences), `name`, `role` (e.g., "Homeowner", "Interior Designer", "Event Planner", "Restaurant Owner", "Office Manager", "Hotel Manager").

Placeholder quotes should be realistic — about custom dimensions, quality, service speed, etc.

**Grid layout:**
- `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 mt-14`

**Each testimonial card:**
- Container: `border border-neutral-700 rounded-lg p-6` (subtle border on dark bg, rounded corners for card feel)
- Star rating row: Map over `rating` count, render filled star SVGs. Use the same star SVG pattern from the hero testimonial overlay: `<svg className="w-4 h-4 fill-current text-accent-foreground" viewBox="0 0 24 24"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>`. Display stars in a `flex gap-0.5 mb-3` row.
- Quote: `text-sm leading-relaxed text-neutral-300` with the quote text (use &ldquo; and &rdquo; for proper quotation marks)
- Author: `mt-4 text-sm` with name in `font-medium text-accent-foreground` and role in `text-neutral-400` separated by a middot (`&middot;`)

**In `src/app/page.tsx`:**
- Add import: `import { TestimonialsSection } from "@/components/home/testimonials-section";`
- Insert `<TestimonialsSection />` between `<WorkSection />` and the existing CTA Banner section
- Final section order: Hero > About > Services > Our Work > Testimonials > CTA Banner
  </action>
  <verify>Run `npm run build` — must exit 0. Verify testimonials-section.tsx exists and page.tsx imports it. Check that 6 testimonial cards are defined in the data array.</verify>
  <done>Testimonials section renders on dark background with 6 client reviews in responsive 3-column grid, each with star ratings, quote text, and author attribution.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 with no TypeScript or build errors
2. `src/components/home/work-section.tsx` exists with 3 project showcase cards
3. `src/components/home/testimonials-section.tsx` exists with 6 testimonials in grid
4. `src/app/page.tsx` imports and renders both new sections
5. Section order on homepage: Hero > About > Services > Our Work > Testimonials > CTA Banner
6. Work section has alternating image/content layouts (even=image-left, odd=image-right)
7. Each project card has category tag, duration tag, and inline testimonial
8. Testimonials section has dark background (`bg-foreground`)
9. Each testimonial card displays star rating using SVG stars
10. Testimonials grid is responsive: 1 col mobile, 2 cols sm, 3 cols lg
</verification>

<success_criteria>
- Build passes with zero errors
- All 5 requirements covered: WORK-01 (showcase cards), WORK-02 (alternating layouts), WORK-03 (category/duration/testimonial per project), TEST-01 (dark background), TEST-02 (responsive grid with stars)
- Design follows established monochrome color system and typography patterns
- No new dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/09-showcase-social-proof/09-01-SUMMARY.md`
</output>
