---
phase: 12-category-navigation-product-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/header.tsx
  - src/components/layout/footer.tsx
  - src/app/products/[productId]/page.tsx
  - src/app/blog/page.tsx
autonomous: true

must_haves:
  truths:
    - "Header navigation shows: Pure Blinds | Products | Blog | Cart (no Configure CTA, no section links)"
    - "Mobile menu shows updated navigation with Products and Blog links"
    - "Product detail page breadcrumb shows Home > Products > Category > Product Name"
    - "Blog placeholder page exists at /blog (no 404)"
    - "Footer includes links to product categories and blog"
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "Updated header with Products and Blog navigation"
      contains: "Products"
    - path: "src/app/products/[productId]/page.tsx"
      provides: "Product detail page with full breadcrumb trail"
      contains: "Breadcrumbs"
    - path: "src/app/blog/page.tsx"
      provides: "Blog placeholder page"
      contains: "Coming Soon"
    - path: "src/components/layout/footer.tsx"
      provides: "Updated footer with product and blog links"
      contains: "/products"
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "/products"
      via: "Link href to products page"
      pattern: "href=\"/products\""
    - from: "src/components/layout/header.tsx"
      to: "/blog"
      via: "Link href to blog page"
      pattern: "href=\"/blog\""
    - from: "src/app/products/[productId]/page.tsx"
      to: "src/components/layout/breadcrumbs.tsx"
      via: "Breadcrumbs component import"
      pattern: "import.*Breadcrumbs"
---

<objective>
Update header navigation, product detail breadcrumbs, footer links, and create blog placeholder.

Purpose: Complete the navigation structure so users can move between Products, Blog, and individual product pages with clear wayfinding.
Output: Updated header/footer with new nav structure, enhanced product detail breadcrumbs, blog placeholder page.
</objective>

<execution_context>
@/Users/robinkonijnendijk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/robinkonijnendijk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/layout/header.tsx
@src/components/layout/footer.tsx
@src/app/products/[productId]/page.tsx
@src/lib/product/catalog.ts
@src/lib/product/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update header navigation and create blog placeholder page</name>
  <files>
    src/components/layout/header.tsx
    src/app/blog/page.tsx
  </files>
  <action>
**Header navigation update** (`src/components/layout/header.tsx`):
- Replace existing `navLinks` array with new structure:
  ```
  const navLinks = [
    { href: "/products", label: "Products" },
    { href: "/blog", label: "Blog" },
  ];
  ```
- Remove the "Configure" CTA button from desktop nav (the `<Link href="/products/custom-textile">` with arrow SVG). Replace that area with just the `<CartIcon />` — no extra CTA button.
- Remove the "Configure" CTA button from mobile menu bottom section. Keep `<CartIcon />` in mobile menu footer area.
- Keep all existing header behavior: scroll-aware background, rounded pill, mobile hamburger animation, backdrop blur.
- Both desktop and mobile menus now show only: Products, Blog (as nav links) + Cart icon.
- The `Link` vs `a` tag logic can be simplified since all nav links are now internal routes (all start with "/"). Use `<Link>` for all.

**Blog placeholder page** (`src/app/blog/page.tsx`):
- Server component (no 'use client')
- Simple placeholder page to prevent 404 when clicking Blog nav link
- Import `Link` from `next/link`
- Use Breadcrumbs component: `[{label: "Home", href: "/"}, {label: "Blog", current: true}]` — import from `@/components/layout/breadcrumbs`
- Page content: subheading "Blog" (muted uppercase), h1 "Our Blog", description "Coming soon. We're working on helpful guides about measuring, installation, and care for your window treatments."
- Match existing page styling: `px-6 py-12 sm:py-16` wrapper, `mx-auto max-w-5xl` container
- Add a link back: "In the meantime, browse our [Products](/products)" as a paragraph with an inline Link
  </action>
  <verify>
Run `npx next build 2>&1 | tail -20` — build succeeds. Verify `/blog` route appears in build output. Check that header.tsx no longer contains "Configure" or "custom-textile" or "About" or "Services" or "Our Work" or "Contact" strings in navLinks.
  </verify>
  <done>
Header shows Products and Blog navigation links with Cart icon. No Configure CTA button. Mobile menu matches desktop nav structure. Blog page at /blog renders without 404.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update product detail breadcrumbs and footer links</name>
  <files>
    src/app/products/[productId]/page.tsx
    src/components/layout/footer.tsx
  </files>
  <action>
**Product detail breadcrumbs** (`src/app/products/[productId]/page.tsx`):
- Import `Breadcrumbs` from `@/components/layout/breadcrumbs`
- Import `getProduct` from `@/lib/product/catalog` (instead of `@/lib/product/data`) — the catalog module is the source of truth and returns full `Product` type with `category` field
- Remove the old inline breadcrumb `<nav>` block
- Add `<Breadcrumbs>` component with items:
  - `{ label: "Home", href: "/" }`
  - `{ label: "Products", href: "/products" }`
  - `{ label: formatCategoryName(product.category), href: "/products/" + product.category }` — format category name by replacing hyphens with spaces and capitalizing words
  - `{ label: product.name, current: true }`
- Create a helper function `formatCategoryName(category: string): string` at top of file (or inline) that converts "rollerblinds" → "Rollerblinds", "venetian-blinds" → "Venetian Blinds"
- Keep everything else identical — the product detail layout, dimension configurator, product details panel, how it works panel

**Footer links update** (`src/components/layout/footer.tsx`):
- Update the `links` array to include product categories and blog:
  ```
  const links = [
    { label: "Rollerblinds", href: "/products/rollerblinds" },
    { label: "Venetian Blinds", href: "/products/venetian-blinds" },
    { label: "Textiles", href: "/products/textiles" },
    { label: "Blog", href: "/blog" },
    { label: "About us", href: "#about" },
    { label: "Contact", href: "#contact" },
  ];
  ```
- Update the link rendering: use `<Link>` from `next/link` for internal routes (starting with "/"), keep `<a>` for anchor links (starting with "#")
- Import `Link` from `next/link` at top of file
- The footer is currently a Server Component (no 'use client') — keep it that way, `Link` works in Server Components
  </action>
  <verify>
Run `npx next build 2>&1 | tail -20` — build succeeds with no type errors. Grep the product detail page for "Breadcrumbs" import. Grep footer for "/products/" links.
  </verify>
  <done>
Product detail pages show full breadcrumb trail: Home > Products > [Category] > [Product Name]. Footer contains links to all product categories and blog. All links are functional.
  </done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. Build output includes `/blog` route
3. Header no longer contains "Configure" CTA or homepage section anchors (About, Services, Our Work, Contact)
4. Product detail breadcrumb includes Products and Category levels
5. Footer includes product category links and blog link
</verification>

<success_criteria>
- Header displays: Pure Blinds (logo/home) | Products | Blog | Cart — matches NAV-01 through NAV-05
- Mobile hamburger menu shows Products and Blog with Cart icon
- Product detail page breadcrumb shows Home > Products > Category > Product Name
- Blog page at /blog does not 404
- Footer has links to product categories (rollerblinds, venetian-blinds, textiles) and blog
</success_criteria>

<output>
After completion, create `.planning/phases/12-category-navigation-product-expansion/12-02-SUMMARY.md`
</output>
